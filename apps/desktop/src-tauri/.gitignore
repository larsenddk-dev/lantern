# Tauri / Rust build output
/target/

# Generated by Tauri at build time (schemas, etc.)
/gen/schemas/

# Platform-specific sidecar binary — regenerated from apps/api via PyInstaller
# (see DESKTOP.md). Not committed because it is large and per-architecture.
/binaries/
