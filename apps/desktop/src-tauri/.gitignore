# Tauri / Rust build output
/target/

# Generated by Tauri at build time (schemas, etc.)
/gen/schemas/

# Platform-specific sidecar resources — regenerated from apps/api via
# PyInstaller (--onedir; see DESKTOP.md). Not committed: large + per-arch.
/binaries/
/sidecar/
